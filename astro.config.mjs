// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // This matches the Britto-Lab GitHub organization's Pages site.
  site: 'https://britto-lab.github.io',
});
